[package]
name = "safeguard-audit-investigation"
description = "Investigation services for the Safeguard audit layer: the case store contract, lifecycle service (open, assign, transition, link records, findings, notes, close), and the projection of every lifecycle step onto investigation events in the audit store."
version.workspace = true
authors.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
keywords.workspace = true
categories.workspace = true
publish = false

[dependencies]
safeguard-audit-authorization = { path = "../authorization", version = "0.1.0" }
safeguard-audit-core = { path = "../audit-core", version = "0.1.0" }
safeguard-audit-events = { path = "../audit-events", version = "0.1.0" }
safeguard-audit-storage = { path = "../storage", version = "0.1.0" }
serde.workspace = true
thiserror.workspace = true

[dev-dependencies]
safeguard-audit-memory-store = { path = "../memory-store", version = "0.1.0" }
[lints]
workspace = true
