[package]
name = "safeguard-audit-reporting"
description = "Reporting services for the Safeguard audit layer: deterministic, reproducible reports (compliance, denied/flagged/approved activity, incidents, investigations, evidence summaries) generated from the audit store with count-only summaries, public-reference rows, and content digests."
version.workspace = true
authors.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
keywords.workspace = true
categories.workspace = true
publish = false

[dependencies]
safeguard-audit-authorization = { path = "../authorization", version = "0.1.0" }
safeguard-audit-core = { path = "../audit-core", version = "0.1.0" }
safeguard-audit-events = { path = "../audit-events", version = "0.1.0" }
safeguard-audit-integrity = { path = "../integrity", version = "0.1.0" }
safeguard-audit-storage = { path = "../storage", version = "0.1.0" }
serde.workspace = true
thiserror.workspace = true

[dev-dependencies]
safeguard-audit-memory-store = { path = "../memory-store", version = "0.1.0" }
serde_json.workspace = true
[lints]
workspace = true
