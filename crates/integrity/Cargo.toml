[package]
name = "safeguard-audit-integrity"
description = "Tamper-evident hashing and verification for the Safeguard audit layer: canonical record digests, chained digests, integrity manifests, and verification/tamper-detection over audit records."
version.workspace = true
authors.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
keywords.workspace = true
categories.workspace = true
publish = false

[dependencies]
safeguard-audit-core = { path = "../audit-core", version = "0.1.0" }
serde_json.workspace = true
sha2.workspace = true
thiserror.workspace = true

[dev-dependencies]
safeguard-audit-memory-store = { path = "../memory-store", version = "0.1.0" }
safeguard-audit-storage = { path = "../storage", version = "0.1.0" }
